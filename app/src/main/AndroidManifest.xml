<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoSort">
        <activity android:name=".SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"/>
        <activity android:name=".SignupActivity"/>
        <activity android:name=".DashboardActivity_streak"/>
        <activity android:name=".RewardsActivity"/>
        <activity android:name=".LeaderboardActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".QuizActivity"/>
        <activity android:name=".WasteDetailsActivity"/>
        <activity android:name=".SortingGameActivity"/>
        <activity android:name=".BusinessIdeasActivity"/>
        <activity android:name=".BusinessDetailActivity"/>
    </application>

</manifest>